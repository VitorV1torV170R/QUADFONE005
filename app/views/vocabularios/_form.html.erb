<%= form_with(model: vocabulario) do |form| %>
  <% if vocabulario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vocabulario.errors.count, "error") %> prohibited this vocabulario from being saved:</h2>

      <ul>
        <% vocabulario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
