<%= form_with(model: vocabulariopalavra) do |form| %>
  <% if vocabulariopalavra.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vocabulariopalavra.errors.count, "error") %> prohibited this vocabulariopalavra from being saved:</h2>

      <ul>
        <% vocabulariopalavra.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :palavra, style: "display: block" %>
    <%= form.text_field :palavra %>
  </div>

  <div>
    <%= form.label :vocabulario_id, style: "display: block" %>
    <%= form.text_field :vocabulario_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
